apiVersion: kubeadm.k8s.io/v1beta4
kind: JoinConfiguration
discovery:
  bootstrapToken:
    token: {{ secrets.kubeadm_token }}
    apiServerEndpoint: {{ cluster.control_plane_endpoint }}
    caCertHashes:
      - sha256:{{ secrets.kubeadm_ca_hash }}
controlPlane:
  localAPIEndpoint:
    advertiseAddress: {{ node.bond_ip | split("/") | first }}
    bindPort: 6443
  certificateKey: {{ secrets.kubeadm_certificate_key }}
nodeRegistration:
  name: {{ node.hostname }}
  criSocket: unix:///run/containerd/containerd.sock
  taints: []
  kubeletExtraArgs:
    - name: node-ip
      value: {{ node.bond_ip | split("/") | first }}
patches:
  directory: /etc/kubernetes/patches
---
apiVersion: kubelet.config.k8s.io/v1beta1
kind: KubeletConfiguration
cgroupDriver: systemd
maxPods: 150
serializeImagePulls: false
seccompDefault: true
allowedUnsafeSysctls:
  - net.ipv4.*
  - net.ipv6.*
