---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - pvc.yaml
  - service.yaml
  - service-master.yaml
  - service-primary-headless.yaml
  - pdb.yaml
  - config/bind.secret.sops.yaml
configMapGenerator:
  - name: bind-config
    files:
      - config/named-primary.conf
  - name: bind-zones
    files:
      - config/zones/db.cbannister.xyz
