apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: bind
spec:
  endpoints:
    - interval: 1m
      path: /metrics
      port: metrics
      scheme: http
      scrapeTimeout: 10s
  jobLabel: bind
  selector:
    matchLabels:
      app.kubernetes.io/name: bind
  namespaceSelector:
    matchNames:
      - network
