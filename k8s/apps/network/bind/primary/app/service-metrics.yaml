apiVersion: v1
kind: Service
metadata:
  name: bind-metrics
  labels:
    app.kubernetes.io/name: bind
spec:
  type: ClusterIP
  selector:
    app: bind
  ports:
    - name: metrics
      port: 9119
      targetPort: metrics
      protocol: TCP
