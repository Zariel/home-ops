apiVersion: v1
kind: Service
metadata:
  name: bind-secondary
spec:
  clusterIP: None
  selector:
    app: bind
    role: secondary
  ports:
    - port: 53
      targetPort: 53
      protocol: UDP
      name: dns-udp
    - port: 53
      targetPort: 53
      protocol: TCP
      name: dns-tcp
