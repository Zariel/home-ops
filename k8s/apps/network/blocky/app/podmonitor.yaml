apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: blocky
  namespace: network
  labels:
    app: blocky
spec:
  selector:
    matchLabels:
      app: blocky
  namespaceSelector:
    matchNames:
      - network
  podMetricsEndpoints:
    - port: metrics
      interval: 15s
      path: /metrics
      scheme: http
