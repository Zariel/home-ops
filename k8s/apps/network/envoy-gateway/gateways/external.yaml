---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/gateway_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: external
  annotations:
    external-dns.alpha.kubernetes.io/target: &hostname external.cbannister.xyz
  labels:
    gateway: external
spec:
  gatewayClassName: envoy-gateway
  addresses:
    - type: IPAddress
      value: &addr 10.45.0.11
  infrastructure:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: *hostname
      io.cilium/lb-ipam-ips: *addr
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.cbannister.xyz"
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.cbannister.xyz"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: cbannister-xyz-tls
