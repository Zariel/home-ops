apiVersion: apps/v1
kind: Deployment
metadata:
  name: &app karma
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: *app
  replicas: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: *app
    spec:
      containers:
        - name: karma
          image: ghcr.io/prymitive/karma:v0.125
          resources:
            requests:
              cpu: "10m"
              memory: "64Mi"
            limits:
              memory: "64Mi"
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: ALERTMANAGER_URI
              value: http://alertmanager.monitoring.svc.cluster.local:9093
            - name: ALERTMANAGER_EXTERNAL_URI
              value: https://alertmanager.cbannister.xyz
            - name: ALERTMANAGER_PROXY
              value: "true"
            - name: FILTERS_DEFAULT
              value: "@state!=suppressed"
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            httpGet:
              path: "/health"
              port: http
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: *app
