apiVersion: v1
kind: Service
metadata:
  name: smartctl-exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: smartctl-exporter
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
