---
apiVersion: v1
kind: Service
metadata:
  name: unpoller
spec:
  selector:
    app.kubernetes.io/name: unpoller
    app.kubernetes.io/instance: unpoller
  ports:
    - name: http
      port: 9130
      targetPort: http
      protocol: TCP