apiVersion: apps/v1
kind: Deployment
metadata:
  name: cross-seed
  annotations:
    reloader.stakater.com/auto: "true"
  labels: &labels
    app.kubernetes.io/component: cross-seed
    app.kubernetes.io/instance: cross-seed
    app.kubernetes.io/name: cross-seed
spec:
  selector:
    matchLabels: *labels
  strategy:
    type: Recreate
  template:
    metadata:
      labels: *labels
    spec:
      containers:
        - name: cross-seed
          image: ghcr.io/cross-seed/cross-seed:6.12.6-0@sha256:0522950529af42ade54924d61bc19b9c3ad999dabeff744439d834bfc5d8525b
          imagePullPolicy: IfNotPresent
          args:
            - daemon
            - --verbose
          env:
            - name: TZ
              value: Europe/London
          ports:
            - name: http
              containerPort: 80
          startupProbe:
            failureThreshold: 3
            periodSeconds: 10
            successThreshold: 1
            tcpSocket:
              port: 80
            timeoutSeconds: 1
          livenessProbe:
            failureThreshold: 3
            periodSeconds: 10
            successThreshold: 1
            tcpSocket:
              port: 80
            timeoutSeconds: 1
          readinessProbe:
            failureThreshold: 3
            periodSeconds: 10
            successThreshold: 1
            tcpSocket:
              port: 80
            timeoutSeconds: 1
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 10m
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - mountPath: /config
              name: config
            - mountPath: /data
              name: data
            - mountPath: /config/config.js
              name: secret-file
              readOnly: true
              subPath: config.js
      enableServiceLinks: false
      restartPolicy: Always
      securityContext:
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        runAsGroup: 568
        runAsNonRoot: true
        runAsUser: 568
        seccompProfile:
          type: RuntimeDefault
        supplementalGroups:
          - 10000
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: cross-seed
        - name: data
          persistentVolumeClaim:
            claimName: bighorse-arr-media
        - name: secret-file
          secret:
            secretName: cross-seed-secret
