apiVersion: v1
kind: Service
metadata:
  name: matter-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: matter-server
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
