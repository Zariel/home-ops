apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thelounge-config
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
