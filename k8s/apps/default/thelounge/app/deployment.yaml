apiVersion: apps/v1
kind: Deployment
metadata:
  name: thelounge
  labels:
    app: thelounge
spec:
  selector:
    matchLabels:
      app: thelounge
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: thelounge
    spec:
      hostname: thelounge
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: thelounge
      securityContext:
        runAsNonRoot: true
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        supplementalGroups: [1000]
        seccompProfile: { type: RuntimeDefault }
      containers:
        - name: thelounge
          image: ghcr.io/thelounge/thelounge:4.4.3@sha256:eb3929136e62afe6ce96578b2ab1e15d2135298886b78f81c1ce5cc148f6b0c4
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: Europe/London
            - name: THELOUNGE_HOME
              value: /config/thelounge
            - name: SQLITE_TMPDIR
              value: /config/thelounge/_tmp
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          ports:
            - containerPort: 9000
              name: web
          startupProbe:
            httpGet:
              port: web
          livenessProbe:
            httpGet:
              port: web
          readinessProbe:
            httpGet:
              port: web
            successThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              memory: 512Mi
          volumeMounts:
            - name: config
              mountPath: /config
