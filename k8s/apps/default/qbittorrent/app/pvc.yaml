apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qbittorrent-config2
spec:
  storageClassName: ceph-block
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
