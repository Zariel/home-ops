apiVersion: apps/v1
kind: Deployment
metadata:
  name: recyclarr
spec:
  selector:
    matchLabels:
      app: recyclarr
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: recyclarr
    spec:
      hostname: recyclarr
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: recyclarr-config
        - name: recyclarr-yml
          configMap:
            name: recyclarr-config
      securityContext:
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
      containers:
        - &container
          name: recyclarr
          image: ghcr.io/recyclarr/recyclarr:latest@sha256:3bc426b72453e76767a7f2144ebcea2870cdf1ecfef6886ee1115c88890f9091
          imagePullPolicy: IfNotPresent
          env:
            - name: DOTNET_EnableDiagnostics
              value: "0"
            - name: DOTNET_gcServer
              value: "1"
          resources:
            requests:
              memory: 200Mi
              cpu: 10m
            limits:
              memory: 200Mi
          volumeMounts:
            - name: config
              mountPath: /config/
              readOnly: false
            - name: recyclarr-yml
              subPath: recyclarr.yaml
              mountPath: /config/recyclarr.yaml
              readOnly: true
            - name: recyclarr-yml
              subPath: sonarr.yaml
              mountPath: /config/configs/includes/sonarr.yaml
              readOnly: true
      initContainers:
        - <<: *container
          name: recyclarr-sync
          args:
            - sync
