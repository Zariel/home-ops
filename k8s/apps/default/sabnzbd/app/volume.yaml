apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sabnzbd-config2
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
