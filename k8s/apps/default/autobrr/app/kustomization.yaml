---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - externalsecret.yaml
  - pvc.yaml
configMapGenerator:
  - name: autobrr-scripts
    files:
      - autoannounce.sh=resources/autoannounce.sh
generatorOptions:
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
