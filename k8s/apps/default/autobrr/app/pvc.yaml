kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: autobrr-config2
spec:
  storageClassName: ceph-block
  resources:
    requests:
      storage: 2Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
