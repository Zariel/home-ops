apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &name autobrr
spec:
  ingressClassName: internal
  rules:
    - host: &host autobrr.cbannister.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: *name
                port:
                  name: http
  tls:
    - hosts:
        - *host
