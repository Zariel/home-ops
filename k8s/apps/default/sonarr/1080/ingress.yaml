apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sonarr-1080
spec:
  ingressClassName: internal
  rules:
    - host: &host sonarr-1080.holywoo.cbannister.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sonarr
                port:
                  name: http
  tls:
    - hosts:
        - *host
