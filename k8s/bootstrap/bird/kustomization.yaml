# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

# Reference Bird manifests from production app directory
# Bird peers with Cilium (iBGP) and advertises to upstream router (OSPF)
resources:
  - ../../apps/kube-system/bird/app
